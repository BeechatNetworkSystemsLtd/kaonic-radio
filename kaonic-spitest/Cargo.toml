[package]
name = "kaonic-spitest"
version = "0.1.0"
edition = "2021"

[dependencies]
linux-embedded-hal = { version = "0.4.0", default-features = false, features = ["spi"] }
embedded-hal = "1.0"
clap = { version = "4", features = ["derive"] }
